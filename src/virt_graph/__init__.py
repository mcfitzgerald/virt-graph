"""
Virtual Graph - Graph-like queries over relational data.

This package enables graph-like queries over enterprise relational data
without requiring migration to a graph database. It uses an LLM to reason
over SQL using a discovered ontology and learned SQL patterns.
"""

__version__ = "0.1.0"
